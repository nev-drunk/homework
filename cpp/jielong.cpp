#include<bits/stdc++.h>

/*
思路：dfs进行深度搜索，对不同的字母进行遍历，直到遍历出元素量最大的
*/

int max = 0;
std::string str[30];//最多30组数据
std::string s;//龙头

int overlap(std::string a, std::string b[])
{
    for (int i = a.size(); i > 0; i--)
    {
        
    }
    
}

int main()
{
    int n;
    std::cin >> n;
    for (int i = 0; i < n; i++)
    {
        std::cin >> str[i];
    }
    std::cin >> s;
    return 0;
}